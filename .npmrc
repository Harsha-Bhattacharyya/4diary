# pnpm configuration
# Strict peer dependency checking enabled (with targeted exceptions below)
strict-peer-dependencies=true

# Auto install peers when appropriate
auto-install-peers=true

# Public hoist pattern for packages that require hoisting
# Only hoist specific packages to maintain pnpm's isolated dependency model
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
@jsr:registry=https://npm.jsr.io
